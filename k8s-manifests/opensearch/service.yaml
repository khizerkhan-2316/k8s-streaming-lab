apiVersion: v1
kind: Service

metadata:
  name: opensearch-service
  namespace: streaming-platform

spec:
  selector:
    app.kubernetes.io/name: opensearch

  ports:
    - name: http
      port: 9200
      targetPort: http